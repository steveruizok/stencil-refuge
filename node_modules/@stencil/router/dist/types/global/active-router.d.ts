import '../stencil.core';
import { LocationSegments, RouterHistory } from './interfaces';
export interface ActiveRouterState {
    location: LocationSegments;
    titleSuffix: string;
    root: string;
    history: RouterHistory;
}
declare const _default: {
    Provider: ({ state, children }: {
        state: ActiveRouterState;
        children?: any[];
    }) => any[];
    Consumer: ({ children }: any) => JSX.Element;
    wrapConsumer: (childComponent: any, fieldList: string | ("location" | "titleSuffix" | "root" | "history")[]) => ({ children, ...props }: any) => JSX.Element;
    injectProps: (childComponent: any, fieldList: string | ("location" | "titleSuffix" | "root" | "history")[]) => void;
};
export default _default;
