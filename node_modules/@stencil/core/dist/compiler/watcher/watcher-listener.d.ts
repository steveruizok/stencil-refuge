import * as d from '../../declarations';
export declare class WatcherListener {
    private config;
    private compilerCtx;
    private dirsAdded;
    private dirsDeleted;
    private filesAdded;
    private filesDeleted;
    private filesUpdated;
    private configUpdated;
    private hasCopyChanges;
    private watchTmr;
    constructor(config: d.Config, compilerCtx: d.CompilerCtx);
    subscribe(): void;
    fileUpdate(filePath: string): void;
    fileAdd(filePath: string): void;
    fileDelete(filePath: string): void;
    dirAdd(dirPath: string): Promise<void>;
    dirDelete(dirPath: string): Promise<void>;
    startRebuild(): void;
    queue(): void;
    resetWatcher(): void;
}
