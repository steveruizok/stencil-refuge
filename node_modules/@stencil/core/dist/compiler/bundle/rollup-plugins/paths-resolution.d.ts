import * as d from '../../../declarations';
import * as ts from 'typescript';
export default function pathsResolver(config: d.Config, compilerCtx: d.CompilerCtx, testTsconfig?: ts.CompilerOptions): Promise<{
    name: string;
    resolveId(importee: string, importer: string): string;
}>;
